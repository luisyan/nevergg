# Never GG website

> Website to check in game status

## Requirements

* [NodeJS](http://nodejs.org/) installed and ready.

## Installation

We need to install the dependencies included in `package.json`:

```bash
$ npm install
```